Каталог нужен для статики, отдаваемой как есть (robots-ассеты, иконки, файлы для
верификации доменов). Пустым git его не сохранит, поэтому лежит этот файл:
без каталога падает postbuild, который копирует public в standalone-сборку.
